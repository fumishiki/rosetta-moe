[package]
name = "nn-ffi"
version = "0.1.0"
edition = "2024"
description = "FFI bridge for MoE Transformer GPU operations"
license = "MIT OR Apache-2.0"
build = "build.rs"

[dependencies]

[build-dependencies]
cc = { version = "1.0", features = ["parallel"] }
