[workspace]
resolver = "2"
members = [
    "rust/nn-core",
    "rust/nn-ffi",
]
exclude = ["benchmarks/rust"]

[workspace.package]
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"

[profile.release]
lto = true
codegen-units = 1
